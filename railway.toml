[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python scripts/generate_data.py && python scripts/build_index.py && uvicorn src.api.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/health"
healthcheckTimeout = 600
restartPolicyType = "ON_FAILURE"
